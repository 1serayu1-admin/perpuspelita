/**
 * CATATAN: File ini TIDAK digunakan sebagai entry point.
 * Entry point aktif adalah src/main.tsx yang di-render oleh ReactDOM.createRoot().
 *
 * File ini sengaja dikosongkan untuk menghindari kebingungan.
 * Jangan import dari file ini.
 */

export {};
